const producto = 'Monitor 20 pulgadas';

console.log(producto)

// Conocer la cantidad de letras de la cadena de texto
console.log(`Uso de length: ${producto.length}`)

// Conocer la posicion que se encuentra, -1 es porque no lo encontro
console.log(`Uso de indexOf(): ${producto.indexOf('pulgadas')}`)

// Conocer si el objeto a buscar existe o no
console.log(`Uso de includes(): ${producto.includes('Monitor')}`)